# Advisory analysis and publishing

Policy advisories are hints only. They never alter a pass or fail verdict.
Remediation guidance is generated from report evidence and should be reviewed by
the application owner before policy changes are made.

The HTTP publisher sends the serialized, redacted report only when a caller
explicitly configures an endpoint and token. It does not configure or host a
reporting service.
